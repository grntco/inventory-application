<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %>></title>
  </head>
  <body>
    <% if (items) { %>
    <ul>
      <% items.map(item => { %>
      <li>
        <h3><%= item.name %></h3>
        <p><%= item.description%></p>
      </li>
      <% })%>
    </ul>
    <% } %>
  </body>
</html>
